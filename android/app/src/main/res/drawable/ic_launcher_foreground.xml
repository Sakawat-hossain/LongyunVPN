<?xml version="1.0" encoding="utf-8"?>
<!--
  LongyunVPN shield. Used as the adaptive launcher-icon foreground (Android 8+)
  and the Android 12+ splash-screen icon. The badge is inset so it stays inside
  the adaptive safe zone / splash circle instead of being clipped by the mask.
  (Legacy launchers on Android 7 and older use the mipmap-*/ic_launcher.png.)
-->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher"
    android:insetLeft="16%"
    android:insetTop="16%"
    android:insetRight="16%"
    android:insetBottom="16%" />
