<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <!--
      Required, not optional. Both services declare
      android:foregroundServiceType="specialUse", and startForegroundCompat()
      passes FOREGROUND_SERVICE_TYPE_SPECIAL_USE on API 34+. Android refuses that
      type without this permission and throws SecurityException out of
      startForeground(), which kills the service the moment it tries to go
      foreground — the app is then left showing "Connecting..." against a service
      that never started, on exactly the devices new enough to enforce it.
      SYSTEM_EXEMPTED above is a different type's permission and does not cover
      specialUse.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!--
      Needed to hand a downloaded APK to the system installer for the in-app
      update. It only lets us *ask* — Android still shows its own install screen
      and the user confirms there; it grants no silent install.

      NOTE FOR DISTRIBUTION: Google Play restricts this permission and treats
      self-updating outside Play as a policy violation. It is here because
      LongyunVPN ships its APKs from GitHub Releases. If this app is ever listed
      on Play, remove this permission and the update path in CommonAction that
      uses it.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
      Per-app split tunnelling needs to list the apps the user can pick from.
      On API 30+ that requires package visibility, and the blunt instrument for
      it is QUERY_ALL_PACKAGES — which Play treats as sensitive and gates behind
      a declaration, with rejection if it isn't justified.

      Declaring the launcher intent instead makes every app the user can
      actually launch visible, which is exactly the set worth showing in an app
      picker, and needs no declaration. The trade-off is that apps with no
      launcher activity (pure background services) no longer appear in the list.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".Application"
        android:banner="@mipmap/ic_banner"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/LongyunVPN">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="clash" />
                <data android:scheme="clashmeta" />
                <data android:scheme="longyunvpn" />

                <data android:host="install-config" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TempActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/TransparentTheme">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.START" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.STOP" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.TOGGLE" />
            </intent-filter>
        </activity>

        <service
            android:name=".TileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/LongyunVPN"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="${applicationId}.permission.RECEIVE_BROADCASTS">
            <intent-filter>
                <action android:name="${applicationId}.intent.action.SERVICE_CREATED" />
                <action android:name="${applicationId}.intent.action.SERVICE_DESTROYED" />
            </intent-filter>
        </receiver>

        <!--
          Grants the system installer read access to the downloaded APK. A
          file:// URI would throw FileUriExposedException on API 24+, so the
          update has to hand over a content:// URI instead. Distinct authority
          from the service module's FilesProvider so the two cannot collide.
          file_paths.xml already exposes the app's files directory, which is
          where the updater saves the download.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          Firebase collects nothing until the user turns it on in Settings.
          These pin both SDKs off at install time, before any Dart code has run
          — without them Crashlytics and Analytics default to enabled and would
          report on first launch, ahead of any consent. FirebaseService then
          enables whichever the user has actually agreed to, and that choice is
          persisted by the SDKs across launches.
        -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>